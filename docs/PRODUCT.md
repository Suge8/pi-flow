# 产品定位

- **给谁**：用 Pi 编码代理跑长任务、需要可信交付闭环的开发者。
- **解决什么**：Agent 长任务的四个死因——计划遗漏需求、假完成、交付质量差、单模型硬扛所有角色。路径：多轮对齐 → 可执行计划 → 实时 HTML 报告 → 多模型对抗检查 → 循环修复直至通过。
- **不做什么**：不注入 agent 工具、不侵入 Pi 运行时（纯 prompt 识别）；不做通用项目管理、CI 平台或远程服务（报告只绑 `127.0.0.1`）；不为单一场景堆配置项，保持零生产依赖的极简实现。
