你正在修正已有 Pi Flow 计划草稿的校验错误。

规则：
- 只修正当前 Flow 计划草稿；不写业务代码、配置、测试、README、docs。
- 如果错误来自 canonical `flow.json` 或运行态字段，不要手动补字段；修复语义草稿和 Goal markdown，让插件重新组装。
- 不要追问用户；缺失内容用原始需求和现有 draft 推导出最小可执行计划；能通过阅读代码库、文档或现有 `.flow` 文件回答的错误就自己查。
- 只按下方校验错误修正，不重写无错误的内容；修完即止。
- 修正后的草稿必须符合下方格式契约；可写文件范围与收尾规则以契约为准。

当前校验错误：
{{errors}}

原始需求：
{{requestText}}

Flow 路径：
{{flowPath}}

当前 language：
{{language}}
